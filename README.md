
# Orbit Wars

**Orbit Wars** — это многопользовательская (или синглплеерная) игра на базе физики орбитального движения. Игроки используют гравитацию небесных тел для маневрирования и уничтожения противников.

> **Статус проекта:** Инициализация (Миграция на агентскую схему)
> **Стек:** Python, Pygame, Orbital Physics

---

## Инструкция для ИИ-Агента (Lead Engineer)

Ты управляешь этим репозиторием. Твои действия регулируются следующими правилами:

1.  **Синхронизация:** Перед любой правкой кода сверяйся с заметками в папке `/Context` в Obsidian.
2.  **Git Flow:** 
    *   Каждая фича — в отдельной ветке `feature/имя-фичи`.
    *   Обязательное создание Pull Request после реализации.
3.  **Приватность:** Не коммить папки `Data/` и `Private/` (настрой `.gitignore`).
4.  **Документирование:** Реализовал физический расчет — обнови `Stack/API_Design.md` или `Structure/Module_Index.md`.

---

## Архитектура базы знаний (Obsidian)

Проект поддерживается через древовидную структуру знаний:

*   **[`/Context`](./Context)** — Игровая механика, расчеты гравитации и ТЗ.
*   **[`/Stack`](./Stack)** — Спецификации Pygame, константы физики и дизайн API.
*   **[`/Structure`](./Structure)** — Карта классов (Planet, Ship, Projectile) и их взаимодействие.
*   **[`/Data`](./Data) [LOCAL]** — Ассеты, логи тестов и сырые идеи.
*   **[`/Private`](./Private) [LOCAL]** — Заметки об ошибках сборки (напр. CMake/библиотеки).

---

## Технический стек и физика

В основе игры лежит закон всемирного тяготения Ньютона, используемый для расчета траекторий:

$$F = G \frac{m_1 m_2}{r^2}$$

Где:
*   $G$ — гравитационная постоянная;
*   $m_1, m_2$ — массы тел;
*   $r$ — расстояние между центрами масс.

Для реализации используется библиотека **Pygame**. Все расчеты векторов производятся в реальном времени.

---

## Быстрый старт

### 1. Подготовка окружения
Убедитесь, что у вас установлен Python 3.10+ и необходимые зависимости:
```bash
pip install pygame
```

### 2. Запуск
```bash
python main.py
```

### 3. Настройка для Агента (GitHub)
Если ты — ИИ-агент, начни с:
1. Создания приватного репозитория (если еще не создан).
2. Настройки `.gitignore` (исключи папки `Data/` и `.obsidian/`).
3. Формирования первой задачи в `Context/Tasks.md`.

---

## 📈 Журнал прогресса (Milestones)

- [x] **M1: Foundation** — Базовая отрисовка небесных тел и игрока.
- [ ] **M2: Gravity Engine** — Корректный расчет орбит и влияния масс.
- [ ] **M3: Combat System** — Реализация стрельбы с учетом искривления траектории.
- [ ] **M4: Multiplayer** — Сетевой режим (или локальный PvP).

---

## ⚠️ Внимание (Known Issues)
Если при установке зависимостей возникают ошибки компиляции C++ компонентов (например, для смежных библиотек анализа данных), убедись, что в системе установлен **CMake** и обновлены **build-essential**. Решения таких проблем фиксировать в `/Private/Environment_Setup.md`.

---
**Разработчик:** [saijo1233]
**AI Architect:** Gemini 3 Flash (Lead Agent)
